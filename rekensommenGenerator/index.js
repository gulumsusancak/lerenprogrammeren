let mingetal = parseInt(prompt("minimum waarde:"));
let maxgetal = parseInt(prompt("maximum waarde:"));

if (mingetal >= maxgetal) {
    console.log("Minimum waarde kan niet groter dan maximumwaarde zijn!!");
} else {

}


